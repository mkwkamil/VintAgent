services:
  vintagent:
    build: .
    image: vintagent:latest
    container_name: vintagent
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped
    shm_size: 256m
    mem_limit: 700m
    cpus: 1.0
